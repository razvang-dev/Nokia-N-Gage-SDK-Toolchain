# HelloNgage

Minimal native Nokia N-Gage / Series 60 1.x application included as a build sanity check and starting template.

Build from the toolchain root:

```bash
source ./env.sh
./scripts/verify-env.sh
./scripts/build-example.sh
```

The application displays `Hello N-Gage!` and exits with the right softkey or key 7.
